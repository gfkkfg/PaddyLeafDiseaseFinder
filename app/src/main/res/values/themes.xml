<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PaddyLeafDiseaseDetection" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PaddyLeafDiseaseDetection" parent="Base.Theme.PaddyLeafDiseaseDetection" />
    <!-- Light Theme -->
        <style name="AppTheme.Light" parent="Theme.AppCompat.DayNight.NoActionBar">
            <!-- Customize your light theme colors -->
            <item name="colorPrimary">#FFFFFF</item>
            <item name="colorPrimaryDark">#000000</item>
            <item name="android:textColor">#000000</item>
            <item name="android:background">#FFFFFF</item>
        </style>

        <!-- Dark Theme (Black and White) -->
        <style name="AppTheme.Dark" parent="Theme.AppCompat.DayNight.NoActionBar">
            <!-- Customize your dark theme colors -->
            <item name="colorPrimary">#000000</item>
            <item name="colorPrimaryDark">#FFFFFF</item>
            <item name="android:textColor">#FFFFFF</item>
            <item name="android:background">#000000</item>
        </style>

</resources>